<p>Hi <%= "buddy".upcase %>!</p>

<h1>Task List</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      Task Name: <%= task[:name] %>; Description: <%= task[:description] %> <%= link_to "(View Details)", show_task_path(task.id) %>
    </li>
  <% end %>
</ul>

<%= link_to "(Add New Task)", new_task_path %>
